[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --tb=short --strict-markers
markers =
    integration: needs a running backend on :8000 and a live database
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
